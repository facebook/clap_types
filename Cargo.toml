[package]
name = "clap_types"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Generate strongly-typed command builders from clap command definitions"
license = "MIT OR Apache-2.0"
repository = "https://github.com/facebook/clap_types"
keywords = ["clap", "cli", "typescript", "flow", "bindings"]
categories = ["command-line-interface", "development-tools"]

[dependencies]
clap = { version = "4.5.20", default-features = false, features = ["std"] }
serde_json = "1"

[features]
default = []
unstable-output-contracts = ["clap/unstable-ext"]

[dev-dependencies]
clap = { version = "4.5.20", default-features = false, features = ["derive", "help", "std"] }

[[example]]
name = "echo-args"
path = "tests/fixtures/echo_args_bin.rs"

[lints.rust]
unsafe_code = "forbid"
unreachable_pub = "warn"
unused_lifetimes = "warn"
unused_macro_rules = "warn"
unused_qualifications = "warn"

[lints.clippy]
dbg_macro = "warn"
todo = "warn"
unwrap_used = "warn"
wildcard_imports = "warn"
